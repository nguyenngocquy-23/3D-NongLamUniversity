import React from "react";
import styles from "./CampusMap.module.css";
import { div } from "framer-motion/client";

const CampusMap = () => {
  return (
    <div className={styles.container}>
      <p className={styles.description}>BẢn đồ đây.</p>
    </div>
  );
};

export default CampusMap;
